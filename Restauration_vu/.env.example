# Production : URL complète de l’API (sans slash final).
# VITE_API_BASE_URL=https://votre-api.example.com

# En développement, le frontend utilise par défaut le proxy Vite `/api` → `http://localhost:3000`
# (voir vite.config.js). Démarrez le backend : `cd ../backend && npm run dev`
